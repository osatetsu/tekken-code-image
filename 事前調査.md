# 事前調査

# 目的と概要

obsidian (markdown editor) のプラグインを作ろうとしている。
コードブロックに記述した内容をDSLに見立て、SVG画像へ変換するものである。
obsidian 公式の mermaid に似たものにしたいと考えている。

コードブロックに記述する内容(DSL)と、対応する図形は既に決まっている。

このプラグインは、鉄拳という格闘ゲームにおける、日本のコミュニティーで一般的なコマンド表記を画像に変換するツールとなる。

# コードブロックの記述ルール

- 1,2,3,4,6,7,8,9: 方向(矢印)の図形になる。数字と対応する方向は以下の通り。
  - 1: 左下
  - 2: 下
  - 3: 右下
  - 4: 左
  - 6: 右
  - 7: 左上
  - 8: 上
  - 9: 右上
- n または N: 方向はニュートラルを示し、星印の図形になる
- LP, RP, LK, RK, WP, WK: 攻撃記号。常に〇が4つ並ぶ図形となる。記述された内容と対応する〇は指定色で塗りつぶす
  - WP: LPとRP両方の意味
  - WK: LKとRK両方の意味
  - +: 例えば LP+RK などと記述すると、LPとRK両方の意味になる。すなわち、WP は LP + RP と同じ意味。WP + WK はLP, RP, LK, RKの4つ全部指定したものになる。
  - `[]`: スライド記号。`[` (開始)と `]` (終了)の間にLP, RP, LK, RKのいずれか複数個を後述のセパレーター記号で区切って記述する。1個だけ記述することは無い。攻撃記号を指定された個数分並べて記述する。
    - 表記例: `[LKRP]`
    - エラーとしたい表記は、`[` や `]` が連続する。 `[` が無いのに `]` が存在する。`[` は存在しても `]` が無い。`[` と `]` の間に数字やnがある。
- `>`: 区切り記号。`>` 記号の図形になる。画像となったときに他の図形との隙間調整、あるいは、区切りとして使う。
  - 連続しての記述 `>>` は無効であり、エラーとする。
  - 行の最後に配置されても良い。
- カンマ、スペース: 対応する図形は無く、単純に無視すれば良い。単純にDSLを人間が読みやすくするためだけに使う。連続して使われた場合も同様。
- "テキスト": ダブルクォート記号で囲まれた部分は、そのままのテキストとして図形になる。テキストの長さは不定である。
  - テキスト内にダブルクォートを含めたい場合はエスケープする。例: `"これはエスケープの\"例\"です"`
  - テキスト内にコロンを含めたい場合はエスケープする。例: `"This is not a \:key."`
  - テキスト内に記述ルール内にある記号(`>#[]`)が含まれていても、そのままテキストとして扱う。前述のとおり `:"` はエスケープが必要である。
- #: 行頭に#記号のある行は、設定を記述する。設定の記法は後述する。
- :英数字: : コロン記号で囲んだ英数字。予約とし、現段階では無視する。将来、絵文字のような形で、アイコンにしたい。
- 設定を除く、DSLの内容は概ね1行に収まり、せいぜい最大200文字程度である。通常、これよりずっと少ない。

特に記述が無い限り、連続して記述してよい。
また、異なる記号を、区切りスペースを間に入れずに並べても良い。

もし、許容する記法において脆弱性となりうる場合は、実装前に報告すること。


## 該当コードブロックの識別

コードブロックに"tekken"がつくと、こちらのプラグインでの画像変換対象としたい。

```tekken
```

## 設定の記法

コロン記号 `:` で区切ったキーバリュー方式とする。
CSS での書き方に近い。

例1:

```
# font-size:14px, padding:8
```

例2:

```
# font-size:14px
# padding:8
```

例1と例2は同じ設定を意味し、1行にカンマで区切って記述しても良いし、複数行に分割して記述しても良いとする。

## コードブロックの記述例

例1: 

```tekken
4LP+RK > 9RK > 3LKRP > 3LKRPLK "(T)" > 66 > 6WP
```

例2: 

```tekken
6n23RP > 6n23RP
```

例3:

```tekken
3RPLP > 6[LKRP] > 3LKRPLP
```

# 図形のルール

- テキスト以外の図形は固定であるため、コード上に埋め込んでおきたい。このとき、idを割り当てておく。配置後のidは、該当SVG内でユニークとなるようにする。連番で良い。
  - 矢印の図形は右向きのものだけ用意し、配置する時に transform で回転させる。
  - スライド記号に対応するSVG図形は `[` のみ埋め込んでおく。配置する時に transform で左右反転させる。
  - 埋め込んでおく図形は外部ツール(inkscapeなど)で作図する。これらのことから、図形を埋め込んでおくソースコードは専用のファイルに分けておきたい。
- 図形は左から右に並べる
- 図形同士は重ならず、パディング領域を図形同士の間に持つ。すなわち、図形同士は少し離して配置する。
- 前述の都合により、画像サイズは固定にできず、DSLを最後まで解析後、あるいは、テキストのレンダリング幅を見積もった後に判明する。
- 画像の背景は透明(100%透過)とする。
- 画像の縁は、マージン領域として事前に指定した幅をあけておき、このマージン領域には何も配置しないこと。


## 攻撃の図形

攻撃の図形のみ特殊であり、circle が4つまとまっていて、それぞれ LP, RP, LK, RK に対応する。これらは id で区別できるようにしている。
埋め込んだ図形としては、`fill=none`としておき、配置時にfill色を決める。コードブロックで指定されないidは白色、指定されたidは黒色(もしくは設定された色)とする。
攻撃図形の例として、下記コードブロックに例を示す。

```
  <g
    id="attack">
    <circle
        id="LP"
        cx="16.598188"
        cy="21.048805"
        r="14"
        fill=none
        stroke="#a3a3a3"
        stroke-width="2.4" />
    <circle
        id="LK"
        cx="20.59819"
        cy="48.829727"
        r="14"
        fill=none
        stroke="#a3a3a3"
        stroke-width="2.4" />
    <circle
        id="RP"
        cx="44.598183"
        cy="15.267878"
        r="14"
        fill=none
        stroke="#a3a3a3"
        stroke-width="2.4" />
    <circle
        id="RK"
        cx="48.598186"
        cy="43.048801"
        r="14"
        fill=none
        stroke="#a3a3a3"
        stroke-width="2.4" />
  </g>
```

> RP(15.3) が LP(21.0) より上にあり、LK/RK も同様に上下が逆転しています。これは意図的な配置（テニスボール配置や格ゲーの慣習）か、それともミスか確認が必要です。

これは意図的な配置である。
来歴としては初代 Playstation のボタン配置からであり、もっと極端に十字に近い形で並んでいた。しかし、現代ではスペースの都合や読みやすさの観点で配置を変更している。


# Obsidian 側での要求

- mermaid での画像のように、画像のどこかに触れると、コードブロックの編集が可能となるようにしたい
- Obsidian での設定を設ける。設定項目は以下の通り。
  - 図形1つの大きさ(デフォルト: 64px)
  - パディング幅(デフォルト: 8px)
  - マージン幅(デフォルト: 8px)
  - テキストのフォントファミリー(デフォルトでは指定なし)
  - フォントサイズ(単位: pt)
  - 攻撃の塗りつぶし色(デフォルトでは黒色)
    - LP
    - RP
    - LK
    - RK
  - デバッグモードの有効、無効
    - デバッグモード時は、各図形のバウンディングボックスとして、赤い rect で囲う。この rect は塗りつぶさない。

図形とフォントサイズの単位が異なる。より良い方式があれば提案されたい。

# 要求

## 中間表現

DSLからSVG画像へ変換するにあたって、下記のような中間表現を用いる。

```typescript
type Node =
  | { type: "arrow"; direction: 1|2|3|4|6|7|8|9 }
  | { type: "neutral" }
  | { type: "attack"; buttons: ("LP"|"RP"|"LK"|"RK")[] }
  | { type: "slide"; buttons: ("LP"|"RP"|"LK"|"RK")[]; reversed: boolean }
  | { type: "separator" }  // ">" 区切り記号
  | { type: "text"; value: string }

type Diagram = {
  nodes: Node[];
  settings?: Partial<Settings>;  // # 行の設定オーバーライド
}
```

理由: 
- SVG生成フェーズからロジックを分離できる
- 各ノードの位置計算（x座標の累積）を段階的に行える
- デバッグモード時にバウンディングボックスを追加しやすくする


## SVG作図

ライブラリを使わず、テンプレート文字列で直接SVGを生成する。

理由:

- Obsidianプラグインのバンドルサイズを最小化できる
- このプロジェクトで必要なSVG要素は `<svg>`, `<g>`, `<use>`, `<rect>`, `<text>`, `<path>` など限定的
- svg-panzoom や svg.js のようなライブラリはDOM操作前提で重い
- Chevrotain + 文字列生成で十分、かつ200ms以内の達成が容易

## 図形(テキスト)のサイズ計算

テキストノードの幅は、Canvas `measureText` を用いて計算する。

## テキストの縦方向の配置位置

テキストと図形の縦方向の揃えについては、図形の下から1/4に baseline がくるようにしたい。
これが困難である場合は、図形とテキストは中央で揃えたい。中央で揃える場合、フォントによってはアンバランスで格好悪いため避けたいが、表示されないよりは良い。


## 図形の埋め込み方法（`<defs>` + `<use>` 方式）

SVGの `<defs>` に各図形を定義し、`<use>` で参照します：

```
<svg>
  <defs>
    <g id="arrow-right"> ... </g>
    <g id="neutral-star"> ... </g>
    <g id="attack-circle"> ... </g>
    <g id="slide-left"> ... </g>
    <g id="separator"> ... </g>
  </defs>
  <!-- 配置例 -->
  <use href="#arrow-right" transform="rotate(135, cx, cy)" x="10" y="10"/>
</svg>
```

- 矢印は右向き（6:右）を基準に、`rotate(angle, cx, cy)` で回転
- スライド記号は `[` のみを定義し、`]` は `transform="scale(-1, 1)"` で反転

## エラーハンドリング

不正なDSLが記述された場合の挙動として、直ちに返還を停止し、 "ERROR: エラー内容" のテキストをSVG画像として表示する。
原則、エラー検出契機は構文解析時とする。エラー検出契機の方針としては、処理がシンプルで高速であることとする。極端にシビアな状況は存在しないため。

エラー内容は、英語で、大雑把に分類されたい。
また、可能ならば、不正、あるいは、異常を検知した文字位置(文字数カウントとしての位置)を表示して欲しい。
文字数のカウントがパフォーマンスに著しく影響する場合は、文字数をカウントせず、また、エラー内容にも文字位置は不要である。

エラー表示のSVG画像における諸設定について
- フォントサイズ: 12pt 固定
- 幅: 可能ならコードブロックと同様
- 高さ: フォントサイズの120%程度(マージンが0の場合を考慮し、少し広めにしておく)
- テキストの縦方向の位置: baseline を考慮し、概ね中央に配置する
- テキストの横方向の位置: 左に1文字程度の空白を開け、左詰めで配置する
- マージンやパディングの設定値は使用しない


## 設定の優先順位

設定の優先度を高い順に記載する。

1. コードブロック内の # 設定
2. Obsidian 全体設定
3. デフォルト値

## 変換パフォーマンス

Obsidianの画面上で、コードブロックが現れてから、画像に変換されるまで、概ね200ミリ秒程度に収まれば問題ない。

# 指示事項

さて、技術選定における未検討、提案待ちの内容を順番に決めていきたい。
また、実装、実現に当たり不足している内容や、矛盾している内容があれば提示されたい。
